<!DOCTYPE html>
<html lang="en">
<head>
    <style>
       body {
                text-align: center;
                background-color: black;
                color: red;
            }
        img{
            width: 250px;
            height: 250px;
            border-radius: 50%;
        }
        input {
            color: blue;
            width: 130px;
            height: 50px;
        }
        div {
            display: flex;
            justify-content: flex-end;
            width: 100%;
            height: 50px;
            flex-direction: row;
        }
        a{
            margin-left: 80px;
            margin-right: 30px;
        }
        .perfil{
            width: 55px;
            height: 55px;
        }
        
    </style>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil</title>
</head>
<body>
    <header>

        <% if (locals.isLogged) { %>
            <div>
                <img class="perfil" src="/images/avatar/<%= locals.userLogged.avatar %> "  alt="">
                <select name="HiUser" id="HiUser">
                    <option value="Hi">Hi <%= locals.userLogged.fullName %> </option>
                    <option value="Logout">Logout</option>
                </select>
         
        <% } else { %>
            <a href="/user/register">Registro</a>

            <a href="/user/login">Login</a>
        </div>

        <% } %>

    </header>
    <h1>Bienvenido a su Perfil </h1>
    <br><br><br>
    
    <figure class=>    
    <img src="/images/avatar/<%= user.avatar %>" alt="">
    <figcaption><%= user.fullName %> </figcaption>
    <figcaption><%= user.email %> </figcaption>
    <figcaption>Su numero de ID: <%= user.id %> </figcaption>
    </figure>
    <br><br><br><br>
    <a href="/user/logout"><input type="button" value="Logout"> </a>

</body>
</html> 